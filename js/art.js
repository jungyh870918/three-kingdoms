/* =========================================================================
 *  인물 삽화 —  tools/pixelize.html 에서 만들어 이 파일을 덮어쓴다.
 *  이름 → data:image/png;base64,...  (95x110, 금색 액자 포함)
 *  여기에 없는 무장은 이름 해시 기반 도트 초상화가 대신 그려진다.
 * ========================================================================= */
const ART = {};
